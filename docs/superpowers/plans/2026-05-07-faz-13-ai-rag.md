# Faz 13 — AI RAG (pgvector) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** AI Asistan'ın cevaplarını ürün katalogu + projeler + SSS'lerden retrieved context ile zenginleştir. **API key olmadan** tüm altyapı kurulu kalsın; key gelince sadece env değiştir + re-index → çalışsın. Resend/Faz 5 patternini takip ediyoruz: provider yoksa graceful skip, AI mevcut davranışını korur.

**Architecture:**
- **Provider abstraction:** `EmbeddingProvider` interface. İki impl:
  - `nullProvider` (default): `embed()` çağrılırsa `null` döner; indexer/retrieve atlanır.
  - `voyageProvider` (env'de `VOYAGE_API_KEY` varsa aktif): Voyage AI `voyage-2` modeli (1024 dim, multilingual, Türkçe iyi).
- `embeddings` tablosu: `doc_type ('product'|'project'|'faq') + doc_id` unique. Aynı doc tekrar embed edilirse upsert.
- **Indexer:** tüm products/projects/faqs'i sırayla embed eder, rate limit'e dikkat (provider'da).
- **Retrieve:** kullanıcı sorgusunu embed et → top-K cosine-similar dökümanları çek (`<=>` operatörü).
- **AI route inject:** retrieved dökümanları system prompt'a inject. Yoksa mevcut akış aynen.
- **Admin UI:** `/admin/ai` — toplam embed sayıları + "Yeniden indeksle" butonu + son run zamanı.
- **Cron:** günde 03:00'da `reindexAll()`. Provider null'sa erken çıkar.

**Tech Stack:**
- pgvector (Supabase native)
- Voyage AI HTTP API (`https://api.voyageai.com/v1/embeddings`) — direct fetch, SDK yok
- `lib/ai/embeddings/*` modülleri

**Tahmini task: 8**

---

## Task 1: pgvector migration + embeddings tablo

**Files:**
- Create: `supabase/migrations/0013_pgvector_embeddings.sql`
- Create: `supabase/migrations/combined_for_paste_v11.sql` (= v10 superset + 0013)
- Modify: `lib/db/types.ts` (Embedding type)

- [ ] **Step 1: Migration**

```sql
-- 0013_pgvector_embeddings.sql
create extension if not exists vector;

create table if not exists public.embeddings (
  id uuid primary key default gen_random_uuid(),
  doc_type text not null check (doc_type in ('product','project','faq')),
  doc_id uuid not null,
  content text not null,
  embedding vector(1024) not null,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now(),
  unique(doc_type, doc_id)
);

create index if not exists embeddings_idx on public.embeddings using ivfflat (embedding vector_cosine_ops) with (lists = 100);
create index if not exists embeddings_doc_idx on public.embeddings(doc_type, doc_id);

-- Sadece admin yazabilir (RLS bypass admin client, ama public read kapalı)
alter table public.embeddings enable row level security;
drop policy if exists "embeddings_admin_only" on public.embeddings;
create policy "embeddings_admin_only" on public.embeddings
  for all using (public.is_admin()) with check (public.is_admin());
```

- [ ] **Step 2: combined_for_paste_v11.sql**

v10 içeriğinin tamamı + 0013 sonuna eklenmiş şekilde. Doğrulama satırına `embeddings_exists`, `pgvector_installed` ekle.

- [ ] **Step 3: lib/db/types.ts'e Embedding type**

```ts
export interface Embedding {
  id: string;
  docType: 'product' | 'project' | 'faq';
  docId: string;
  content: string;
  metadata: Record<string, unknown>;
  createdAt: string;
}
```

- [ ] **Step 4: db:apply ile uygula**

```powershell
npm run db:apply
npx tsx scripts/db-check.ts
```

`db-check.ts`'e `embeddings` tablosu kontrolü ekle. Doğrulama: `embeddings: true`.

- [ ] **Step 5: Commit**

```powershell
git add supabase/migrations/0013_pgvector_embeddings.sql supabase/migrations/combined_for_paste_v11.sql lib/db/types.ts scripts/db-check.ts
git commit -m "feat(db): add pgvector + embeddings table for AI RAG"
```

---

## Task 2: Embedding provider abstraction

**Files:**
- Create: `lib/ai/embeddings/provider.ts`
- Create: `lib/ai/embeddings/null-provider.ts`
- Create: `lib/ai/embeddings/voyage-provider.ts`
- Create: `lib/ai/embeddings/index.ts`
- Test: `tests/lib/ai/embeddings/null-provider.test.ts`

- [ ] **Step 1: Provider interface**

```ts
// lib/ai/embeddings/provider.ts
export interface EmbeddingProvider {
  readonly slug: string;
  readonly dimensions: number;
  /** Returns null if provider not configured (e.g., API key missing). */
  embed(text: string): Promise<number[] | null>;
  /** Batch embed; returns array same length as input, with null for failures. */
  embedBatch(texts: string[]): Promise<Array<number[] | null>>;
}
```

- [ ] **Step 2: Null provider (default)**

```ts
// lib/ai/embeddings/null-provider.ts
import type { EmbeddingProvider } from './provider';

export const nullProvider: EmbeddingProvider = {
  slug: 'null',
  dimensions: 1024,
  async embed(): Promise<number[] | null> {
    return null;
  },
  async embedBatch(texts) {
    return texts.map(() => null);
  },
};
```

- [ ] **Step 3: Voyage provider**

```ts
// lib/ai/embeddings/voyage-provider.ts
import 'server-only';
import type { EmbeddingProvider } from './provider';

const VOYAGE_URL = 'https://api.voyageai.com/v1/embeddings';
const MODEL = 'voyage-2';

async function callVoyage(texts: string[]): Promise<Array<number[] | null>> {
  const key = process.env.VOYAGE_API_KEY;
  if (!key) return texts.map(() => null);
  try {
    const res = await fetch(VOYAGE_URL, {
      method: 'POST',
      headers: {
        'authorization': `Bearer ${key}`,
        'content-type': 'application/json',
      },
      body: JSON.stringify({ input: texts, model: MODEL }),
    });
    if (!res.ok) {
      console.error('[voyage] HTTP', res.status, await res.text());
      return texts.map(() => null);
    }
    const json = (await res.json()) as { data?: Array<{ embedding: number[] }> };
    return texts.map((_, i) => json.data?.[i]?.embedding ?? null);
  } catch (e) {
    console.error('[voyage] threw', e);
    return texts.map(() => null);
  }
}

export const voyageProvider: EmbeddingProvider = {
  slug: 'voyage',
  dimensions: 1024,
  async embed(text) {
    const [r] = await callVoyage([text]);
    return r ?? null;
  },
  async embedBatch(texts) {
    return callVoyage(texts);
  },
};
```

- [ ] **Step 4: Provider selector**

```ts
// lib/ai/embeddings/index.ts
import 'server-only';
import type { EmbeddingProvider } from './provider';
import { nullProvider } from './null-provider';
import { voyageProvider } from './voyage-provider';

export function getEmbeddingProvider(): EmbeddingProvider {
  if (process.env.VOYAGE_API_KEY) return voyageProvider;
  return nullProvider;
}

export type { EmbeddingProvider };
```

- [ ] **Step 5: Failing test**

```ts
// tests/lib/ai/embeddings/null-provider.test.ts
import { describe, it, expect } from 'vitest';
import { nullProvider } from '@/lib/ai/embeddings/null-provider';

describe('nullProvider', () => {
  it('embed returns null', async () => {
    expect(await nullProvider.embed('test')).toBeNull();
  });
  it('embedBatch returns array of nulls matching input length', async () => {
    const r = await nullProvider.embedBatch(['a', 'b', 'c']);
    expect(r).toEqual([null, null, null]);
  });
  it('exposes slug and dimensions', () => {
    expect(nullProvider.slug).toBe('null');
    expect(nullProvider.dimensions).toBe(1024);
  });
});
```

- [ ] **Step 6: Test pass**

- [ ] **Step 7: Build clean + commit**

```powershell
git add lib/ai/embeddings/ tests/lib/ai/embeddings/
git commit -m "feat(ai): add embedding provider abstraction (null + voyage)"
```

---

## Task 3: Doc serializers

**Files:**
- Create: `lib/ai/embeddings/serializers.ts`
- Test: `tests/lib/ai/embeddings/serializers.test.ts`

Her doc tipini "embed edilebilir" düz Türkçe metne çevirir. AI bu metni kaynak olarak görür.

- [ ] **Step 1: Failing test**

```ts
import { describe, it, expect } from 'vitest';
import { productToText, projectToText, faqToText } from '@/lib/ai/embeddings/serializers';

describe('serializers', () => {
  it('productToText includes name and key fields', () => {
    const t = productToText({
      name: 'Solar Panel 540W',
      brand: 'Trina',
      price: 12500,
      warrantyYears: 25,
      shortDescription: 'Yüksek verim',
      description: 'Detaylı açıklama',
      category: 'Paneller',
    });
    expect(t).toContain('Solar Panel 540W');
    expect(t).toContain('Trina');
    expect(t).toContain('25');
    expect(t).toContain('12500');
  });

  it('projectToText includes title + capacity + location', () => {
    const t = projectToText({
      title: 'İstanbul Çatı Kurulumu',
      type: 'konut',
      location: 'İstanbul',
      capacityKwp: 5.4,
      description: null,
    });
    expect(t).toContain('İstanbul');
    expect(t).toContain('5.4');
  });

  it('faqToText includes question + answer + category', () => {
    const t = faqToText({
      question: 'Garantisi kaç yıl?',
      answer: '25 yıl',
      category: 'garanti',
    });
    expect(t).toContain('Garantisi kaç yıl');
    expect(t).toContain('25 yıl');
  });
});
```

- [ ] **Step 2: Implementation**

```ts
// lib/ai/embeddings/serializers.ts
export interface ProductSeed {
  name: string;
  brand: string | null;
  price: number;
  warrantyYears: number | null;
  shortDescription: string | null;
  description: string | null;
  category: string | null;
}

export interface ProjectSeed {
  title: string;
  type: 'konut' | 'ticari' | 'tarim';
  location: string;
  capacityKwp: number;
  description: string | null;
}

export interface FaqSeed {
  question: string;
  answer: string;
  category: string;
}

export function productToText(p: ProductSeed): string {
  const parts: string[] = [`Ürün: ${p.name}`];
  if (p.category) parts.push(`Kategori: ${p.category}`);
  if (p.brand) parts.push(`Marka: ${p.brand}`);
  parts.push(`Fiyat: ${p.price} TL`);
  if (p.warrantyYears) parts.push(`Garanti: ${p.warrantyYears} yıl`);
  if (p.shortDescription) parts.push(`Özet: ${p.shortDescription}`);
  if (p.description) parts.push(`Detay: ${p.description}`);
  return parts.join('\n');
}

export function projectToText(p: ProjectSeed): string {
  const parts: string[] = [`Proje: ${p.title}`, `Konum: ${p.location}`, `Tip: ${p.type}`, `Kapasite: ${p.capacityKwp} kWp`];
  if (p.description) parts.push(`Detay: ${p.description}`);
  return parts.join('\n');
}

export function faqToText(f: FaqSeed): string {
  return `Soru: ${f.question}\nCevap: ${f.answer}\nKategori: ${f.category}`;
}
```

- [ ] **Step 3: Test pass + commit**

```powershell
git add lib/ai/embeddings/serializers.ts tests/lib/ai/embeddings/serializers.test.ts
git commit -m "feat(ai): add doc serializers for products/projects/faqs"
```

---

## Task 4: Indexer (reindexAll)

**Files:**
- Create: `lib/ai/embeddings/indexer.ts`

- [ ] **Step 1: Implementation**

```ts
// lib/ai/embeddings/indexer.ts
import 'server-only';
import { createAdminClient } from '@/lib/supabase/admin';
import { getEmbeddingProvider } from './index';
import { productToText, projectToText, faqToText } from './serializers';

const BATCH_SIZE = 16;

export interface ReindexResult {
  provider: string;
  products: number;
  projects: number;
  faqs: number;
  errors: number;
  skipped: boolean;
}

export async function reindexAll(): Promise<ReindexResult> {
  const provider = getEmbeddingProvider();
  if (provider.slug === 'null') {
    return { provider: provider.slug, products: 0, projects: 0, faqs: 0, errors: 0, skipped: true };
  }
  const sb = createAdminClient();
  let products = 0;
  let projects = 0;
  let faqs = 0;
  let errors = 0;

  // PRODUCTS
  const { data: prods } = await sb.from('products').select('id,name,brand,price,warranty_years,short_description,description,category_id');
  const prodList = (prods ?? []) as Array<Record<string, unknown>>;
  for (let i = 0; i < prodList.length; i += BATCH_SIZE) {
    const slice = prodList.slice(i, i + BATCH_SIZE);
    const texts = slice.map((r) =>
      productToText({
        name: r.name as string,
        brand: (r.brand as string | null) ?? null,
        price: Number(r.price),
        warrantyYears: (r.warranty_years as number | null) ?? null,
        shortDescription: (r.short_description as string | null) ?? null,
        description: (r.description as string | null) ?? null,
        category: null,
      }),
    );
    const vectors = await provider.embedBatch(texts);
    for (let j = 0; j < slice.length; j++) {
      const v = vectors[j];
      if (!v) { errors++; continue; }
      const r = slice[j]!;
      await sb.from('embeddings').upsert(
        {
          doc_type: 'product',
          doc_id: r.id as string,
          content: texts[j]!,
          embedding: v,
          metadata: { name: r.name },
        },
        { onConflict: 'doc_type,doc_id' },
      );
      products++;
    }
  }

  // PROJECTS
  const { data: projs } = await sb.from('projects').select('id,title,type,location,capacity_kwp,description');
  const projList = (projs ?? []) as Array<Record<string, unknown>>;
  for (let i = 0; i < projList.length; i += BATCH_SIZE) {
    const slice = projList.slice(i, i + BATCH_SIZE);
    const texts = slice.map((r) =>
      projectToText({
        title: r.title as string,
        type: r.type as 'konut' | 'ticari' | 'tarim',
        location: r.location as string,
        capacityKwp: Number(r.capacity_kwp),
        description: (r.description as string | null) ?? null,
      }),
    );
    const vectors = await provider.embedBatch(texts);
    for (let j = 0; j < slice.length; j++) {
      const v = vectors[j];
      if (!v) { errors++; continue; }
      const r = slice[j]!;
      await sb.from('embeddings').upsert(
        {
          doc_type: 'project',
          doc_id: r.id as string,
          content: texts[j]!,
          embedding: v,
          metadata: { title: r.title },
        },
        { onConflict: 'doc_type,doc_id' },
      );
      projects++;
    }
  }

  // FAQS
  const { data: fqs } = await sb.from('faqs').select('id,question,answer,category');
  const fqList = (fqs ?? []) as Array<Record<string, unknown>>;
  for (let i = 0; i < fqList.length; i += BATCH_SIZE) {
    const slice = fqList.slice(i, i + BATCH_SIZE);
    const texts = slice.map((r) =>
      faqToText({
        question: r.question as string,
        answer: r.answer as string,
        category: r.category as string,
      }),
    );
    const vectors = await provider.embedBatch(texts);
    for (let j = 0; j < slice.length; j++) {
      const v = vectors[j];
      if (!v) { errors++; continue; }
      const r = slice[j]!;
      await sb.from('embeddings').upsert(
        {
          doc_type: 'faq',
          doc_id: r.id as string,
          content: texts[j]!,
          embedding: v,
          metadata: { question: r.question },
        },
        { onConflict: 'doc_type,doc_id' },
      );
      faqs++;
    }
  }

  return { provider: provider.slug, products, projects, faqs, errors, skipped: false };
}
```

- [ ] **Step 2: Build clean + commit**

```powershell
git add lib/ai/embeddings/indexer.ts
git commit -m "feat(ai): add reindexAll for products/projects/faqs"
```

---

## Task 5: Retrieve query

**Files:**
- Create: `lib/ai/embeddings/retrieve.ts`

- [ ] **Step 1: Implementation**

Supabase client raw SQL desteklemediği için RPC function gerekli ya da admin client'la direkt query. En kolay: Supabase'de `match_embeddings` RPC function tanımla ya da `.rpc()` çağrısı.

Daha pratik: Supabase RPC function'ı v11 migration'ında zaten ekleyelim (Task 1'e dahil et). v11'e ek olarak:

```sql
-- v11 migration sonuna ekle
create or replace function public.match_embeddings(query_embedding vector(1024), match_count int default 5)
returns table (
  doc_type text,
  doc_id uuid,
  content text,
  metadata jsonb,
  similarity float
)
language sql stable
security definer set search_path = public
as $$
  select doc_type, doc_id, content, metadata, 1 - (embedding <=> query_embedding) as similarity
  from public.embeddings
  order by embedding <=> query_embedding
  limit match_count;
$$;
```

(Task 1 SQL'ine ekleneceğini not olarak yaz.)

```ts
// lib/ai/embeddings/retrieve.ts
import 'server-only';
import { createAdminClient } from '@/lib/supabase/admin';
import { getEmbeddingProvider } from './index';

export interface RetrievedChunk {
  docType: 'product' | 'project' | 'faq';
  docId: string;
  content: string;
  metadata: Record<string, unknown>;
  similarity: number;
}

export async function retrieveContext(query: string, k = 5): Promise<RetrievedChunk[]> {
  const provider = getEmbeddingProvider();
  if (provider.slug === 'null') return [];
  const vec = await provider.embed(query);
  if (!vec) return [];
  const sb = createAdminClient();
  const { data } = await sb.rpc('match_embeddings', { query_embedding: vec, match_count: k });
  return ((data ?? []) as Array<Record<string, unknown>>).map((r) => ({
    docType: r.doc_type as 'product' | 'project' | 'faq',
    docId: r.doc_id as string,
    content: r.content as string,
    metadata: (r.metadata as Record<string, unknown>) ?? {},
    similarity: Number(r.similarity),
  }));
}
```

- [ ] **Step 2: Commit**

```powershell
git add lib/ai/embeddings/retrieve.ts
git commit -m "feat(ai): add retrieveContext via match_embeddings RPC"
```

---

## Task 6: AI route'a context inject

**Files:**
- Modify: `app/api/ai/chat/route.ts`
- Modify: `lib/ai/system-prompt.ts` (varsa, yoksa içine entegre)

- [ ] **Step 1: Mevcut route'u oku**

`app/api/ai/chat/route.ts` mevcut. AI çağrı yapısına bak: kullanıcı mesajı, system prompt, history.

- [ ] **Step 2: retrieveContext çağrısı ekle**

```ts
import { retrieveContext } from '@/lib/ai/embeddings/retrieve';

// Inside POST handler, BEFORE Anthropic call:
const lastUserMessage = messages[messages.length - 1]?.content ?? '';
const context = await retrieveContext(typeof lastUserMessage === 'string' ? lastUserMessage : '', 5);

let contextBlock = '';
if (context.length > 0) {
  const sections = context.map((c) => `[${c.docType}] ${c.content}`).join('\n\n---\n\n');
  contextBlock = `\n\n## Bilgi Tabanı (kullanıcı sorusuna cevaplarken bu kaynaklardan yararlan)\n\n${sections}\n\n---\n\nYukarıdaki bilgi tabanını kaynak olarak kullan ama açıkça referans verme. Türkçe ve doğal cevap ver.`;
}

// Inject into system prompt:
const finalSystemPrompt = systemPrompt + contextBlock;
```

- [ ] **Step 3: Build clean**

- [ ] **Step 4: Commit**

```powershell
git add app/api/ai/chat/route.ts
git commit -m "feat(ai): inject retrieved context into AI chat system prompt"
```

---

## Task 7: Admin re-index UI + cron

**Files:**
- Create: `app/admin/ai/page.tsx`
- Create: `lib/server-actions/admin/reindex.ts`
- Create: `components/admin/reindex-button.tsx`
- Create: `app/api/cron/reindex/route.ts`
- Modify: `vercel.json` (yeni cron entry)
- Modify: `components/admin/sidebar.tsx` (AI link ekle)

- [ ] **Step 1: Server action**

```ts
'use server';
import 'server-only';
import { revalidatePath } from 'next/cache';
import { requireAdmin } from '@/lib/auth/server';
import { reindexAll } from '@/lib/ai/embeddings/indexer';

export type ReindexActionResult =
  | { ok: true; provider: string; products: number; projects: number; faqs: number; errors: number; skipped: boolean }
  | { ok: false; error: string };

export async function reindexAction(): Promise<ReindexActionResult> {
  await requireAdmin();
  try {
    const r = await reindexAll();
    revalidatePath('/admin/ai');
    return { ok: true, ...r };
  } catch (e) {
    console.error('[admin] reindex failed', e);
    return { ok: false, error: 'Re-index sırasında hata oluştu.' };
  }
}
```

- [ ] **Step 2: Reindex button (client)**

`SyncButton` pattern (Faz 11'den) — pending state, sonuç mesajı.

- [ ] **Step 3: Page**

`/admin/ai`:
- Toplam embed sayıları (per type) — Supabase'den `count`
- Provider durumu (`null` ise "API key eksik" warn)
- Reindex butonu

- [ ] **Step 4: Cron route**

`/api/cron/reindex` — `CRON_SECRET` Bearer auth + `reindexAll()`. `vercel.json`'a `0 3 * * *` ekle.

- [ ] **Step 5: Sidebar link**

"AI Bilgi Tabanı" — `/admin/ai`. Brain icon (lucide-react'tan) ya da Sparkles.

- [ ] **Step 6: Build + commit**

```powershell
git commit -m "feat(admin): add AI knowledge base page + cron reindex"
```

---

## Task 8: Final verification + completion report

**Files:**
- Create: `docs/superpowers/plans/2026-05-07-faz-13-completion.md`

- [ ] **Step 1: Test + build**

```powershell
npm test -- --run
npm run build
```

230+/230+ expected.

- [ ] **Step 2: Manuel test (key olmadan)**

- `/admin/ai` → "Provider: null, embed yok" mesajı görünmeli
- "Yeniden indeksle" butonu → "Skipped: API key yok" döner
- AI chat normal çalışmalı (context yok ama hata yok)

- [ ] **Step 3: Completion report**

- [ ] **Step 4: Commit + push**

---

## Self-Review

- [ ] Provider abstraction sayesinde key olmadan tüm akış çalışıyor (graceful skip)
- [ ] embeddings RLS: sadece admin
- [ ] match_embeddings RPC SECURITY DEFINER (anon kullanıcı da retrieve yapabilir, ama embed olmadan boş döner)
- [ ] retrieveContext null vector'ü kontrol edip erken çıkar
- [ ] AI route context yoksa mevcut davranışını korur
- [ ] Indexer batch + upsert (idempotent)
- [ ] Reindex button her seferinde tüm doc'ları yeniden embed eder (incremental yok — Faz 14)

## Kapsam Dışı (Faz 14+)

- Incremental indexing (sadece değişen doc'lar)
- Hybrid search (keyword + vector)
- Re-ranker (Cohere Rerank)
- Per-user knowledge base
- Streaming retrieval
- Tool use (function calling)

## Anahtar Eklendiğinde

User `.env.local`'a `VOYAGE_API_KEY=...` ekleyince:
1. Dev server restart
2. `/admin/ai` → "Yeniden indeksle" tıkla
3. ~5-30 saniye sonra embeddings dolar
4. AI chat artık RAG'lı cevap veriyor
